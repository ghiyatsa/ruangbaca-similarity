# Dependensi khusus pengembangan & CI (tidak dipakai di image produksi).
# Lihat Dockerfile — hanya requirements.txt yang diinstal ke runtime.

# Linter + formatter Python. Versi DI-PIN karena Ruff menambah/mengubah
# aturan antar rilis; tanpa pin, CI bisa tiba-tiba gagal tanpa perubahan kode.
ruff==0.16.8

# Test runner
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
